{
  "name": "algorithms-project",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postStartCommand": "bash -lc 'if git symbolic-ref -q HEAD >/dev/null; then git pull --rebase --autostash; else echo \"Detached HEAD; skipping auto-pull\"; fi'"
}

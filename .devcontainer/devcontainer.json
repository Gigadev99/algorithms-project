{
  "name": "algorithms-project",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postStartCommand": "bash -lc 'if git symbolic-ref -q HEAD >/dev/null; then git pull --rebase --autostash; else echo \"Repository is in detached HEAD state; automatic pull skipped. Check out a branch to enable automatic pull\"; fi'"
}
